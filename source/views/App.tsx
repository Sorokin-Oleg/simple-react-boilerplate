import React from "react";

import styles from "./App.module.css";

const App: React.FC = () => {
  return(
    <h1 className={styles.title}>This is simple boilerplate for React</h1>
  );
};

export default App;
